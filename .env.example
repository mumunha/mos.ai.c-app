# Database Configuration (Railway PostgreSQL)
# ⚠️  REPLACE with your PUBLIC Railway PostgreSQL URL (not internal!)
# Get it from: Railway Dashboard → PostgreSQL Service → Connect Tab → Public Network
# Format: postgresql://postgres:password@containers-us-west-XXX.railway.app:PORT/railway
# Railway typically doesn't require SSL, so we explicitly disable it
DATABASE_URL=postgresql://postgres:your-password@your-host.railway.app:your-port/railway?sslmode=disable

# OpenAI Configuration
# Replace with your actual OpenAI API key
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# JWT Secret for authentication
# Generate a secure random string for production
JWT_SECRET=your-jwt-secret-key-here

# Telegram Bot Configuration (optional)
TELEGRAM_BOT_TOKEN=your-telegram-bot-token
TELEGRAM_WEBHOOK_SECRET=your-webhook-secret

# Application Configuration
NEXT_PUBLIC_APP_URL=https://your-app.up.railway.app
NODE_ENV=production

# Authentication
BCRYPT_ROUNDS=10

# Admin Configuration
# Email address for admin access to admin panel
ADMIN_EMAIL=your-admin-email@example.com